name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Updating and Installing the basic packages
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Updating Repositories
        run: sudo apt-get update && sudo apt-get upgrade -y
        
      - name: Installing dependencies
        run: sudo apt-get install -y git python3-pip

      - name: Installing Python dependencies
        run: pip install -r requirements.txt

# Deploying are ainsible playbook
  deploy:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Deploying ansible-playbook
        run: ansible-playbook deploy-ansible.yml -i inventory.ini
        working-directory: ansible

# Verify everything is properly running
# Deploying are ainsible playbook
  verify:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Verifying Docker Containers are running
        run: docker ps

      #- name: Connecting to the webpage of our container
      #  run: firefox -u "http://localhost:"
